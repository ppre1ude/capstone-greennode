# Agent Instructions

## 기본 원칙

- 응답은 기본적으로 한국어로 작성한다.
- 작업 전 현재 브랜치와 `git status`를 확인하고, 사용자 변경을 되돌리지 않는다.
- 문서, 백로그, 검증 결과가 바뀌면 관련 문서를 함께 갱신한다.

## 커밋 메시지 규칙

- 커밋 전 `git log -8 --pretty=format:"%h%n%B%n---"`로 최근 메시지 흐름을 확인한다.
- 제목은 conventional commit 형식을 따른다. 예: `feat: ...`, `fix: ...`, `docs: ...`, `test: ...`, `refactor: ...`, `chore: ...`.
- 메시지는 제목 포함 3-5줄로 작성한다.
- 제목과 본문 사이에는 빈 줄 1개를 둔다.
- 본문 줄 사이에는 빈 줄을 넣지 않는다.
- 제목과 본문은 한글 중심으로 작성하되, API 이름, 파일명, 옵션명 같은 기술 식별자는 영어를 허용한다.
- 각 줄은 명사형으로 끝낸다. 예: `구현`, `추가`, `보강`, `정리`, `반영`, `분리`, `갱신`, `검증`, `기록`.
- `~한다`, `~했다`, `~합니다`, `~함` 같은 서술형 또는 한다체 종결을 쓰지 않는다.
- 커밋 후 `git log -1 --pretty=format:"%h%n%B"`로 실제 저장된 제목/본문 줄바꿈을 확인한다.

예시:

```text
feat: Post-MVP 프론트 계약 구현

multi-object 대표 후보 선택과 selectedDetectionId 전송 경로 추가
서버 알림 sync fallback과 명시 reason fixture gate 보강
백엔드 논의 필요 blocker와 VM 검증 결과 문서 분리
```
